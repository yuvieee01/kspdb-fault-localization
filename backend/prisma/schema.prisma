// This is your Prisma schema file.
// Models will be added in step 2 (Prisma schema + migrations + seed).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
